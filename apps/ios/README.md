# iOS location notice

The active Xcode project is at [`apps/Ios-app/Raichu`](../Ios-app/Raichu/README.md).

This lowercase directory is retained only to prevent old links from becoming ambiguous. Do not add source code or a `package.json` here.

Start with the [canonical iOS platform guide](../../docs/platforms/ios.md).
